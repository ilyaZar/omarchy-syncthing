# Design archive

`Panel.minimal.qml` is the original compact status-and-actions concept. It is
kept for comparison only; `manifest.json` continues to load the active
`Panel.qml` from the repository root.
